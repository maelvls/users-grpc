---
# Source: users-grpc/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: users-grpc
  labels: { app.kubernetes.io/name: users-grpc }
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector: { app.kubernetes.io/name: users-grpc }
---
# Source: users-grpc/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-grpc
  labels: { app.kubernetes.io/name: users-grpc }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: users-grpc }
  template:
    metadata:
      labels: { app.kubernetes.io/name: users-grpc }
    spec:
      containers:
        - name: users-grpc
          image: "maelvls/users-grpc:1.0.0"
          env:
            - { name: LOG_FORMAT, value: json }
            - { name: PORT, value: "8000" }
          ports:
            - name: grpc
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            exec:
              command: ["/bin/grpc-health-probe", "-addr=:8000"]
            initialDelaySeconds: 10
          readinessProbe:
            exec:
              command: ["/bin/grpc-health-probe", "-addr=:8000"]
            initialDelaySeconds: 5

---
# Source: users-grpc/templates/ingress.yaml
